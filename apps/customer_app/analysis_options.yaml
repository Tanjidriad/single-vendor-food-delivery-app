# Customer app analyzer config.
#
# Shared rules live in the repo-root analysis_options.yaml so customer_app and
# rider_app stay consistent. Add app-specific overrides below the include.
include: ../../analysis_options.yaml

# Lint rules can be suppressed per-line with `// ignore: name_of_lint` or
# per-file with `// ignore_for_file: name_of_lint`. Prefer fixing over
# suppressing. App-specific rule overrides go under `linter:` here.
