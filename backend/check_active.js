const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.riderAssignment.findMany({ where: { riderId: '14b1347d-d98e-4f77-94f8-b2f25bac14ae', status: { notIn: ['CANCELLED', 'EXPIRED', 'REJECTED'] } }, include: { order: true } }).then(a => { console.log(JSON.stringify(a, null, 2)); prisma.$disconnect(); }).catch(e => { console.error(e); prisma.$disconnect(); })
