import { NestFactory } from '@nestjs/core'; import { AppModule } from './src/app.module'; import { RealtimeService } from './src/gateways/realtime.service'; import { NotificationsService } from './src/modules/notifications/notifications.service'; import { PrismaService } from './src/prisma/prisma.service'; async function bootstrap() { const app = await NestFactory.createApplicationContext(AppModule); const realtime = app.get(RealtimeService); const notifications = app.get(NotificationsService); const prisma = app.get(PrismaService); const rider = await prisma.riderProfile.findFirst({ where: { isOnline: true }, include: { user: true } }); if (!rider) { console.log('No online rider'); return; } const payload = { assignmentId: 'test-id', orderId: 'test-order-id', orderNumber: 'TEST-1234', pickupLat: 23.8, pickupLng: 90.4, dropLat: 23.81, dropLng: 90.41, estimatedPayout: 50, cashToCollect: 100, paymentMethod: 'COD', expiresAt: new Date(Date.now() + 60000).toISOString() }; realtime.emitAssignmentCreated(rider.user.id, payload); await notifications.sendToUser(rider.user.id, 'Test Push', 'This is a test push notification', { type: 'assignment:created', assignmentId: 'test-id', orderId: 'test-order-id', pickupLat: '23.8', pickupLng: '90.4' }); console.log('Sent to', rider.user.id); await app.close(); } bootstrap();
